/**
 * Database Config
 */
const config = {
  host: 'localhost',
  port: 2424,
  username: 'root',
  password: 'rootpwd',
  // To enable the liveQuery() method
  useToken: true
}

module.exports = config